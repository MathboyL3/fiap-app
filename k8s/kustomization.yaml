apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Aplica todos os manifestos da Oficina de uma vez:
#   kubectl apply -k app/k8s/
resources:
  - 00-namespace.yaml
  - 10-configmap.yaml
  - 20-secret.yaml
  - 30-postgres.yaml
  - 40-api.yaml
  - 50-hpa.yaml
